import React, { useContext } from 'react'
import Notes from "./notes";
import AddNote from './AddNote';

const Home = () => {
    return (
        <div>

            <Notes />
        </div>
    )
}

export default Home
